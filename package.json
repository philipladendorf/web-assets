{
  "name": "web-assets-skill",
  "version": "1.0.0",
  "description": "Generate production-ready visual assets for web projects using AI",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "npx tsx --env-file=.env skills/web-assets/scripts/generate.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@google/genai": "^0.14.0",
    "postcss": "^8.5.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.0.0",
    "vitest": "^4.0.18"
  }
}
